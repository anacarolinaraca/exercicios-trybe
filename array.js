let menu = ['Home', 'Serviços', 'Portfólio', 'Links'];
let menuServices = menu.length;
let indexOfPortfolio = menu.indexOf();
menu.push('Contato');

console.log(menuServices);
console.log(indexOfPortfolio);
console.log(menu);